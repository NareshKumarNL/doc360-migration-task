This project demonstrates a simple document migration workflow where content from a Microsoft Word document is converted into HTML and then prepared to be uploaded as an article to Document360 using its API.

The goal of the task is to simulate a knowledge base migration process, where documentation from one format is programmatically transformed and integrated into a documentation platform
